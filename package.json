{
  "name": "server-with-vue-ssr",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/server/index.ts",
    "build": "bun build src/server/index.ts --outfile build/server.js --target=bun",
    "preview": "bun build/server.js"
  },
  "dependencies": {
    "elysia": "latest",
    "vue": "latest"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "latest",
    "bun-types": "latest",
    "vite": "latest"
  }
}
